# E2E

Playwright/browser tests will live here.

The first required scenario is:

1. open a real `.hpl`;
2. render it with React Flow;
3. move/connect/edit a transform;
4. edit config/metadata in native React UI;
5. save and reload;
6. verify the core path never enters RAP `/ui`.
